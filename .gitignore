# Build artifacts
server/exra-server

# Rust build artifacts (can be 100MB+)
target/
**/target/
*.pdb
*.rlib
*.rmeta
android/app/build/
android/.gradle/
android/build/
android/**/build/
android/**/.gradle/
dashboard/.next/
dashboard/node_modules/
node_modules/

# OS files
.DS_Store
Thumbs.db

# Env files
.env
.env.local
.env.production
.env.staging

# Deploy — contains server keys, build scripts, pre-built packages
# Not needed for code review by investors/grant committees
deploy/
deploy.sh

# Branch audit artifacts
.branch-audit/

# Grant documents (kept locally, shared separately)
grant/

# IDEs / agent tooling
.vscode/
.idea/
.claude/
*.swp
*.bak

# Landing build artefacts
landing/.next/
landing/node_modules/
landing/out/
