# Build artifacts
server/exra-server
server/exra-server-linux
server/exra-server-linux-*
server/*.exe
server/main
server/kilonet
server/oracle
server/gateway

# Rust build artifacts (can be 100MB+)
target/
**/target/
*.pdb
*.rlib
*.rmeta
android/app/build/
android/.gradle/
android/build/
android/**/build/
android/**/.gradle/
dashboard/.next/
dashboard/node_modules/
node_modules/

# OS files
.DS_Store
Thumbs.db

# Env files
.env
.env.local
.env.production
.env.staging

# Deploy — содержит пароли и собранные артефакты, не для code review
deploy/
deploy.sh
# Но сами скрипты сборки/деплоя — в репо
!deploy/build.sh
!deploy/server-deploy.sh
!deploy/env.sample
!deploy/auto-deploy.py

# Branch audit artifacts
.branch-audit/

# Grant documents (kept locally, shared separately)
grant/

# IDEs / agent tooling
.vscode/
.idea/
.claude/
*.swp
*.bak

# Landing build artefacts
landing/.next/
landing/node_modules/
landing/out/
