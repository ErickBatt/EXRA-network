# Команды для проверки статуса сервера
# SSH: ssh root@103.6.168.174
# Пароль: Kukusa13Kro333

# 1. Проверить какие бинарники есть
cd /root/exra/server
ls -lh *.exe exra-server* server* main 2>/dev/null

# 2. Проверить запущенные процессы
ps aux | grep -E 'exra|server' | grep -v grep

# 3. Проверить последние логи
tail -50 server.log

# 4. Если бинарник называется по-другому, запустить его
# Например, если это server.exe:
# nohup ./server.exe > server.log 2>&1 &

# Или если это main:
# nohup ./main > server.log 2>&1 &

# 5. После запуска проверить
ps aux | grep server
curl -X POST http://localhost:8080/api/tma/auth \
  -H 'Content-Type: application/json' \
  -d '{"init_data":"test"}'

# Должен вернуть: {"error":"invalid telegram initData"}
# Это значит сервер работает правильно!
